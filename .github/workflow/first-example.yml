name : first workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome
        run: echo "Welcome to the first workflow"
      
      - name : list files
        run: ls
      
      - name: Read files
        run: cat README.md